# Recruitmens

- CPU: 4+ vCPUs

- Memory: 16+GB RAM

- Storage: 1TB


## Install Dependencies

First install python3

Then create a virtualenv:

`python3 -m venv venv`

`source venv/bin/activate`

`uv`:
`pip install uv`

